package Oops.Inheritance;

//Inheritance allows a child class (subclass) to reuse the properties and methods of a parent class (superclass).
//        👉 It promotes code reusability.

public class Person {
    String name;

    void sayHello() {
        System.out.println("Hello I am " + name);
    }
}
